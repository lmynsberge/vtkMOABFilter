#include <iostream>
#include <fstream>
#include <string>

int main()
{
  std::ifstream infile("Mats.txt");
  std::string line;

  while (std::getline(infile, line))
    {
      std::cout << "The file said, '"<< line << "'.\n";
    }
  return 0;
}
